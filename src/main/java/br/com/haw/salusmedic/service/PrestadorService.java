package br.com.haw.salusmedic.service;

import org.springframework.stereotype.Service;

@Service
public class PrestadorService {
	
/*	@Autowired
	PrestadorDao prestadorDao;
	@Autowired
	UsuarioDao usuarioDao;
	public Prestador getPrestadorLogado(){
		Authentication autenticado = SecurityContextHolder.getContext().getAuthentication();
		Usuario novoUsuario = usuarioDao.findOneByLogin(autenticado.getName());
		return prestadorDao.findAByUsuario(novoUsuario);
	}*/
}
